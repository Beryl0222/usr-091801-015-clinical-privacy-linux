# 诊疗隐私用途管控

本项目管理电子病历访问、教学观摩、科研导出和现场影像的用途授权。策略依据患者关系、业务目的、字段范围、知情同意版本和有效期判定。紧急救治可短时解封，但必须记录理由并进入事后复核。

导出资料只在本服务登记内容哈希、授权链、操作者、时间和交付对象，原始影像由医疗系统保存。水印验证支持密钥轮换。事件样例包括同意撤回、缓存过期、违规拍摄报告和同源导出追踪。

运行 `python3 service.py --check` 检查基础配置，或使用 `python3 service.py --port 8000` 启动健康端点。
